// Test struct field validation

struct Config {
    port: int | string
    debug: bool
}

main {
    print("Testing struct field validation...")

    // This should error - bool is not int | string for port field
    Config {
        port: true,
        debug: false
    }
}
