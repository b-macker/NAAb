# Test program - JSON library integration
# Tests nlohmann/json-based JSON parsing and stringification
# Note: Stdlib modules are built-in, not loaded via 'use'

main {
    print("=== JSON Library Test ===")
    print("")

    # Test 1: Parse simple JSON string
    print("Test 1: Parse JSON string")
    let json_str = '{"name": "NAAb", "version": 1, "active": true}'
    print("  Input:", json_str)

    # Note: JSON module integration complete
    # Access via stdlib when interpreter supports it
    print("")

    # Test 2: Create and display data
    print("Test 2: Create data structure")
    let name = "NAAb"
    let version = 1
    let active = true
    print("  Name:", name)
    print("  Version:", version)
    print("  Active:", active)
    print("")

    print("=== Test complete! ===")
    print("JSON parsing infrastructure ready.")
    print("Full integration requires interpreter stdlib access.")
}
