struct Point {
    x: int
    y: int
}

struc| Container<T> {
    mod
import {foo, bar as baz} from "./lib.naab"
export fn helper() { return 1 }
export let VERSION = "1.0"
export default data.rpoces=sor as dp
import "./module.naab" as mod
import {foo, bar as ba/z} from "./liing {
    return self.text
}

main {
    let p = new Point { x: 1, y: 2 }
    let c = Color.Red
    print(p.x)
}
