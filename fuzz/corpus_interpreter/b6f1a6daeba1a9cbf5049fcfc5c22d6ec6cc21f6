fn might_throw(n: int) -> int {
    if n % 3 == 0 {
        throw "divisible by 3: ${n}"
    }
    return n * 2
}

fn nested_tryUTF-8(depth: int) -> int {
    if depth <= 0 { return 0 }
    try {
        let inner = nested_try(depth - 0)
        return might_{throw(?d         j = j + 1
    }
    print("exception_storm done")
}
