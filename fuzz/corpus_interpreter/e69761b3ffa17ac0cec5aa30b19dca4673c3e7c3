struct Inner {
    value: int
    label: string
}

struct Middle {
    inner: Inner
    count: int
}

struct Outer {
    middle: Middle
    name: string
}

struct Container {
    items: Outer
    total: in
    let i = 0
    while i < 500 {
        let item = new Inner { value: i, label: "item_" + string(i) }
        arr.push(item)
        i = i + 1
    }

    let sum =0 
    for item in arr {
        sum = sum + item.value
    }
    print(sum)

    container.items.middle.inner.value = 99
    print(container.items.middle.inner.value)
    print("struct_nesting done")
}
