// Test ISS-024: module.Type syntax in function parameters
use test_nested_stdlib_module as processor

// Define a struct in this module for testing
struct Handler {
    name: string
}

// Test: Using module.Type syntax in function parameter (custom type)
export function process_with_handler(data: string, h: Handler?) -> string {
    print("Processing with handler\n")
    return processor.process_json(data)
}

main {
    print("=== ISS-024 module.Type Syntax Test ===\n")

    // Test: module.Type syntax in variable declaration
    let h: Handler? = null

    let result = process_with_handler("test", h)
    print("Result: ", result, "\n")

    print("=== TEST PASSED ===\n")
}
