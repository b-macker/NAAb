// Test: Nested module path with alias
// Expected: Should parse correctly and resolve module

// This tests: use data.processor as dp
// The module path "data.processor" should be parsed correctly
// And alias "dp" should work

use math_utils as m

main {
    print("=== Testing Nested Module with Alias ===\n")

    // Test with short alias
    let sum = m.add(100, 200)
    print("m.add(100, 200) = ", sum, "\n")

    print("=== Nested module alias test complete ===\n")
}

// Note: To fully test nested paths like "data.processor",
// we would need to create a naab_modules/data.naab file first
