use array as arr

main {
    print("Minimal test starting...")

    # Test 1: Basic variable
    let x = 42
    print("x = ", x)

    # Test 2: Array
    let nums = [1, 2, 3]
    print("nums = ", nums)

    # Test 3: Array assignment
    nums[0] = 99
    print("after nums[0]=99: ", nums)

    # Test 4: Array sort (without lambda)
    let sorted = arr.sort(nums)
    print("sorted: ", sorted)

    # Test 5: Python
    print("Python:")
    <<python
    print("  Python works!")
    >>

    # Test 6: JavaScript
    print("JavaScript:")
    <<javascript
    console.log("  JS works!");
    >>

    print("Minimal test complete!")
}
