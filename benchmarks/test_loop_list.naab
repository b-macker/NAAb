# Test loop with list instead of range

main {
    print("Test: For loop with list")

    let items = [1, 2, 3, 4, 5]

    for item in items {
        print("Item: ", item)
    }

    print("Loop complete")
}
