[package]
name = "naab-sys"
version = "0.1.0"
edition = "2021"
authors = ["NAAb Project"]
description = "Rust FFI bindings for NAAb Block Assembly Language"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2"
