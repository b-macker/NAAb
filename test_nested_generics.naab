// Test nested generic types like list<dict<string, string>>

use io
use array

main {
    io.write("Testing nested generic types parsing...\n")

    // Test that list<dict<string, string>> type parses correctly
    let data: list<dict<string, string>> = []

    io.write("✅ Parsed list<dict<string, string>> type successfully\n")

    // Test with dict values
    let dict1 = {"name": "Alice", "role": "Developer"}
    let dict2 = {"name": "Bob", "role": "Designer"}

    data = array.push(data, dict1)
    data = array.push(data, dict2)

    io.write("✅ Created nested data structure\n")
    io.write("  Length: ", array.length(data), "\n")
    io.write("  First item name: ", data[0]["name"], "\n")
}
