# Test ISS-006: C++ Polyglot Return Value
# Status in ISSUES.md: ❌ Failed - C++ return value fails compilation (std headers issue)
# Also leads to null safety error when assigning to non-nullable float

main {
    print("=== Testing ISS-006: C++ Polyglot Return Value ===")

    # Test 1: Return value from C++ (avoiding std headers)
    print("Test 1: Return float from C++ (no std dependencies)")
    let result = <<cpp
    return 3.14159;
    >>
    print("  Result:", result)

    # Test 2: Return int from C++
    print("Test 2: Return int from C++")
    let num = <<cpp
    return 42;
    >>
    print("  Number:", num)

    print("VERDICT: Check if values are returned correctly")
}
