# Test ISS-011: File Module
# Status in ISSUES.md: ✅ Resolved
# Functions: fs.read, fs.write, fs.append, fs.delete

use file as fs

main {
    print("=== Testing ISS-011: File Module ===")

    let test_file = "/data/data/com.termux/files/home/.naab/language/test_file_issue_011.txt"

    # Test 1: fs.write
    print("Test 1: fs.write")
    fs.write(test_file, "Hello World")
    print("  File written")

    # Test 2: fs.read
    print("Test 2: fs.read")
    let content = fs.read(test_file)
    print("  Content:", content)

    # Test 3: fs.append
    print("Test 3: fs.append")
    fs.append(test_file, "\nAppended line")
    let content2 = fs.read(test_file)
    print("  Content after append:", content2)

    # Test 4: fs.delete
    print("Test 4: fs.delete")
    fs.delete(test_file)
    print("  File deleted")

    print("VERDICT: If all functions work, file module is resolved")
}
