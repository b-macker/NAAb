# Test ISS-014: Range Operator (..)
# Status in ISSUES.md: ❌ Missing
# Claim: I implemented this today (2026-01-22)
# Test: Verify if range operator actually works

main {
    print("=== Testing ISS-014: Range Operator ===")

    # Test 1: Basic range (0..5)
    print("Test 1: Basic range 0..5")
    for i in 0..5 {
        print("  i =", i)
    }

    # Test 2: Range with variables
    print("Test 2: Range with variables")
    let start = 10
    let end = 15
    for i in start..end {
        print("  i =", i)
    }

    # Test 3: Range in arithmetic
    print("Test 3: Accumulation")
    let sum = 0
    for i in 1..11 {
        sum = sum + i
    }
    print("  Sum 1..10 =", sum, "(expected 55)")

    print("VERDICT: Range operator works? Testing...")
}
