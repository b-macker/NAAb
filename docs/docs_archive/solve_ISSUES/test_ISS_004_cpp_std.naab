# Test ISS-004: C++ Polyglot with std headers
# Status in ISSUES.md: ❌ Failed - std features fail due to missing header injection
# Documentation claims "works without manual headers"

main {
    print("=== Testing ISS-004: C++ Polyglot std Headers ===")

    # Test 1: C++ with std::cout (no manual headers)
    print("Test 1: C++ block with std::cout (no headers)")
    <<cpp
    std::cout << "Hello from C++ std::cout!" << std::endl;
    >>

    # Test 2: C++ with std::vector (no manual headers)
    print("Test 2: C++ block with std::vector (no headers)")
    <<cpp
    std::vector<int> nums = {1, 2, 3};
    std::cout << "Vector size: " << nums.size() << std::endl;
    >>

    print("VERDICT: If you see C++ output above, automatic headers work!")
}
