# Test ISS-001: Generics Instantiation
# Status in ISSUES.md: ❌ Failed - `new Box<int>` causes parse error
# Expected: Expected '{' for struct literal. Got: '<'

struct Box<T> {
    value: T
}

main {
    print("=== Testing ISS-001: Generics Instantiation ===")

    # Test 1: Try to instantiate generic struct with type parameter
    print("Test 1: new Box<int> { value: 42 }")
    let box_int = new Box<int> { value: 42 }
    print("  Result:", box_int.value)

    print("VERDICT: If you see this, generics instantiation works!")
}
