# Test ISS-005: JS Polyglot Return Value
# Status in ISSUES.md: ❌ Failed - Capturing return from JS block results in null

main {
    print("=== Testing ISS-005: JS Polyglot Return Value ===")

    # Test 1: Return string from JS
    print("Test 1: Capture string from JS")
    let greeting = <<javascript
    "Hello from JavaScript"
    >>
    print("  Type of result:", greeting)
    print("  Value:", greeting)

    # Test 2: Return number from JS
    print("Test 2: Capture number from JS")
    let number = <<javascript
    42
    >>
    print("  Value:", number)

    # Test 3: Return array from JS
    print("Test 3: Capture array from JS")
    let arr = <<javascript
    [1, 2, 3, 4, 5]
    >>
    print("  Array:", arr)

    print("VERDICT: Check if values are null or actual data")
}
