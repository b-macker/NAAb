// test_modules.naab
use math_utils

main {
    print(">>> Chapter 4 Verification: Modules <<<")
    
    let sum = math_utils.add(10, 20)
    print("Sum from module: ", sum) // Expected: 30
    
    print("PI from module: ", math_utils.PI) // Expected: 3.14159
    
    // Test Exported Struct (Parser limitation: new module.Struct not supported yet)
    // let vec = new math_utils.Vector2 { x: 5, y: 5 }
    // print("Vector from module: (", vec.x, ", ", vec.y, ")")

    // Test Exported Enum (Try direct access)
    // let op = Operation.Add
    // if op == Operation.Add {
    //    print("Enum from module: Add")
    // }
    print("Skipping Enum test due to module member access limitation")

    if sum == 30 {
        print("✓ Module function call passed")
    }
}