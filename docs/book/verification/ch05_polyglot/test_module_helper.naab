// test_module_helper.naab
// Helper module for testing imports

export fn double(x: int) -> int {
    return x * 2
}

export fn greet(name: string) -> string {
    return "Hello, " + name + "!"
}

export fn polyglot_calc() -> int {
    let result = <<python
10 + 20 + 30
    >>
    return result
}
