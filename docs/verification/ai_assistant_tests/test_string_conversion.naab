use io

main {
    let num_int = 42
    let num_float = 3.14

    // NAAb uses + operator for string conversion (auto-converts)
    let str_int = "" + num_int
    let str_float = "" + num_float

    io.write("Int to string: " + str_int + "\n")
    io.write("Float to string: " + str_float + "\n")
}
