# Test ISS-012: Env Module
# Status in ISSUES.md: ✅ Resolved
# Functions: env.get, env.set_var

use env as env

main {
    print("=== Testing ISS-012: Env Module ===")

    # Test 1: env.set_var
    print("Test 1: env.set_var")
    env.set_var("TEST_VAR_ISS_012", "test_value")
    print("  Variable set")

    # Test 2: env.get
    print("Test 2: env.get")
    let value = env.get("TEST_VAR_ISS_012")
    print("  Value:", value)

    # Test 3: Get existing env var
    print("Test 3: Get existing env var (PATH)")
    let path = env.get("PATH")
    print("  PATH exists:", path != null)

    print("VERDICT: If all functions work, env module is resolved")
}
