# Test program - Block loading from registry
# This tests loading actual blocks from the SQLite registry

use BLOCK-CPP-00001 as create_util
use BLOCK-PY-00001 as api_response

main {
    print("Testing block loading system")
    print("Blocks should be loaded from the registry")
    print("Check console output above for block load status")
}
