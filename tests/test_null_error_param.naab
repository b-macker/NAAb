// Test null safety error: passing null to non-nullable parameter

fn greet(name: string) {
    print("Hello, ", name)
}

main {
    print("Testing null parameter error...")
    greet(null)  // Should error
}
