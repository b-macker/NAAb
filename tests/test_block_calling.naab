# Test program - Calling blocks as functions
# This tests calling loaded blocks

use BLOCK-CPP-00001 as create_util
use BLOCK-PY-00001 as api_response

main {
    print("Testing block calling system")
    print("")

    # Check block types
    print("Type of create_util:", type(create_util))
    print("Type of api_response:", type(api_response))
    print("create_util =", create_util)
    print("api_response =", api_response)
    print("")

    # Call blocks (will show code snippets)
    print("Calling C++ block:")
    let cpp_result = create_util()
    print("")

    print("Calling Python block:")
    let py_result = api_response()
}
