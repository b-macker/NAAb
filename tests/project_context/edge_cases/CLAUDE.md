# Project Rules

## Language Preferences
- Always use Python
- Prefer Go for concurrency
- Never use shell

## Banned Functions
- Never use eval()
- Don't use os.system()

## Style
- Use 4 spaces for indentation
- No console.log in production

## Code Examples

```python
# This is an EXAMPLE — do NOT extract rules from here
eval("2+2")
os.system("rm -rf /")
always use JavaScript
never use Python
```

    # This indented block should also be skipped
    always use Rust
    never use Go

## More rules after code block
- Prefer Ruby for string processing

> Consider using Haskell (this blockquote should be SKIPPED)
