// Module that imports stdlib (testing ISS-022 nested imports)
use json
use string

export function process_json(data: string) -> string {
    print("Processing JSON data\n")
    let upper = string.upper(data)
    return upper
}

export function parse_config(json_str: string) -> dict<string, string> {
    print("Parsing config from JSON\n")
    return {}
}
