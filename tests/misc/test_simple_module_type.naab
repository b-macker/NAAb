// Minimal test for module type syntax
use test_module_types_def as types

function test() -> types.Config {
    return types.Config {
        name: "test"
        port: 8080
    }
}

main {
    print("Testing module type in return type\n")
    let config = test()
    print("Name: ", config.name, "\n")
}
