// Main orchestrator script
use test_error_module as errmod

function orchestrate() {
    print("Starting orchestration...\n")

    // This will fail deep in the module
    let result = errmod.deeply_nested()

    print("This won't print\n")
}

main {
    print("=== Testing Error Reporting Across Modules ===\n")
    orchestrate()
}
