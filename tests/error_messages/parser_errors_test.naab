// Test parser error messages and hints
// This file intentionally contains an error to test error reporting
// EXPECTED: This test should FAIL with a helpful error about 'fn main()' vs 'main {}'

use io

// ERROR: Using 'fn main()' instead of 'main {}'
// Expected: Hint about using 'main {}' block
fn main() {
    io.write("This should fail with a helpful error message\n")
}
