# Test program for enhanced error messages
# This program contains intentional errors to test "Did you mean?" suggestions

main {
    # Define some variables
    let count = 10
    let total = 100
    let username = "Alice"

    # Test 1: Typo in variable name (count -> cout)
    print(cout)  # Should suggest: Did you mean 'count'?
}
