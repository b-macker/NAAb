// E-08: Eval evasion via reflection — Python globals()
// Expected: exit 3 — governance detects obfuscated eval

main {
    let result = <<python
fn_name = "ev" + "al"
result = globals()["__builtins__"].__dict__[fn_name]("1+1")
result
>>
    print(result)
}
