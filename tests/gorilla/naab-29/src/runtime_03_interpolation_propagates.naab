// R-03: String concatenation propagates taint
// Expected: exit 3 — concatenating with tainted var produces tainted result
use env
use file

main {
    let tainted = env.get("HOME")
    let msg = "path is: " + tainted
    file.write("output.txt", msg)
}
