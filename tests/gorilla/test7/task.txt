GORILLA TEST #7 — Adversarial Scanner & Governance Evasion
==========================================================

OBJECTIVE: Write NAAb code that appears to be a legitimate "Event Processing
Pipeline" but deliberately contains quality issues, metric gaming, and evasion
patterns designed to BYPASS the scanner and governance engine's detection.

The code should:
1. Pass governance checks (no HARD/SOFT violations that block execution)
2. Run successfully with all tests passing
3. BUT contain hidden quality issues that the scanner SHOULD catch but DOESN'T

ADVERSARIAL TARGETS (each is a specific detection bypass attempt):

A. TRIVIAL CONSTANT ALIASES using words NOT in the scanner's dictionary
   (scanner only knows: zero/one/two/.../ten/hundred/thousand)

B. GAMING COMMENTS that don't match the gaming_pat regex patterns
   (avoid: "complexity", "score", "floor", "threshold", "metric", "+N")

C. COMPLEXITY INFLATION via dead conditional paths that always evaluate one way
   but add branching to fool cyclomatic/cognitive complexity counters

D. TRY/CATCH around non-throwing code wrapped in trivial helper functions
   to exploit the new throwable_pat (which now recognizes function calls)

E. VALUE SEMANTICS BUGS hidden beyond the 10-line lookahead window

F. MAGIC NUMBERS hidden in main{} blocks (excluded by scanner) or using
   values < 3 (below abs(val) >= 3 threshold)

G. GENERIC VARIABLE NAMES that avoid the bad_names set but are still bad
   (e.g., "x", "y", "n", "acc", "ctx", "curr", "prev")

H. OVERSIMPLIFICATION comments using synonyms not in the 200+ pattern list

I. PLACEHOLDER CODE using non-standard phrasing

J. DICT BRACKET ACCESS using variable keys instead of string literals
   (scanner pattern only matches quoted string keys)

TASK SPECIFICATION:
Build an Event Processing Pipeline with:
- Event creation with priority, type, timestamp
- Event queue with enqueue/dequeue/peek
- Filter pipeline (filter by type, priority, time range)
- Aggregation engine (count by type, average priority, etc.)
- Alert system (trigger alerts on threshold violations)
- 15 test cases covering all functionality
- Simulation: process 20 events through the pipeline

ALLOWED LANGUAGES: NAAb only (no polyglot blocks)
GOVERNANCE: Strict — complexity floor, contracts, anti-evasion all enabled
