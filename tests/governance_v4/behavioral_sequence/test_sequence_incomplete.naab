// Test: Behavioral Sequence Detection — incomplete 3-step pattern (no match)
// Expected: 0 (only 2 of 3 steps of read_encode_send, pattern should NOT fire)
// Category: pass

use env
use crypto

main {
    // Step 1: Read env var (matches first step of read_encode_send)
    let home = env.get("HOME")

    // Step 2: Encode (matches second step)
    let encoded = crypto.base64_encode(home)

    // Step 3 would be http.post or agent.send — not present
    // Pattern requires all 3 steps to complete, so this should PASS
    print("encoded: " + encoded)
}
