# Test BLOCK-JSON - Proper block loading
# JSON functionality as a loadable block, not hardcoded stdlib

use BLOCK-JSON as json

main {
    print("=== Testing BLOCK-JSON (Loadable Block) ===")
    print("")

    print("Block loaded successfully!")
    print("JSON module is now available as 'json'")
    print("")

    print("Note: C++ block function calling not yet implemented")
    print("Next step: Extend interpreter to call C++ block functions via dlopen/dlsym")
}
