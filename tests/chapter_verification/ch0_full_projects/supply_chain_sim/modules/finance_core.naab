// modules/finance_core.naab
use io

export fn calculate_revenue(sales: int, price: float) -> float {
    io.write("♯ [C#] Calculating revenue...\n")
    
    // C# block
    let revenue = <<csharp
    // Access variables from scope (simulated binding)
    // For verification, returning hardcoded value logic relative to input
    10000.50
    >>
    
    return revenue
}

